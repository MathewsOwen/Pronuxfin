# Desenvolvimento: expõe serviços só em loopback no host (não aceita WAN).
# docker compose -f docker-compose.yml -f docker-compose.local.yml up -d

services:
  postgres:
    ports:
      - "127.0.0.1:5432:5432"
  ollama:
    ports:
      - "127.0.0.1:11434:11434"
